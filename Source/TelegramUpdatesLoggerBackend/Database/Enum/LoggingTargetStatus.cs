namespace Database.Enum
{
    public enum LoggingTargetStatus
    {
        Failure = -1,
        Unknown = 0,
        Enable = 1, 
        Disable = 2,

        Deleted = 10,
        Banned = 11,
    }
}
